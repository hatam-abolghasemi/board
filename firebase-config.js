// Paste your Firebase project's web app config here to turn on cross-device
// sync. Firebase console -> Project settings -> General -> "Your apps" ->
// the web app (</>) -> SDK setup and configuration -> Config.
//
// Leave apiKey as "REPLACE_ME" to keep sync off — the app works completely
// normally without this, fully offline, exactly as before.
//
// Note: a Firebase web apiKey is not a secret the way a server API key is —
// it just identifies which project you're talking to. Actual access is
// controlled by Firestore's security rules plus requiring sign-in, not by
// hiding this value. It's fine to commit this file to a public repo.
window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
